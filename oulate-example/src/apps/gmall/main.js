import ReactDOM from 'react-dom';
import React from 'react';

import { VERSION_CODE } from '../../utils';
import './style.scss';

console.log(VERSION_CODE);

ReactDOM.render(
  <h1>Hello Word GMall</h1>,
  document.getElementById('root')
);
